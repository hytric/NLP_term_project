7.3 map 부분 너무 작아서 안보임 plot 을 다시해야할 것 같은데 가능해?

한번에 통합해서 다시 뽑기

지금 현재 부록 데이터가 너무 간략하게 작성되어있음. A.1,2 결과 값을 table 로 정리해서 제시하고

A3 또한 동일하게 표로 정리한다.

A4 부터는 B 로변경해서 tail, head, all 을 table 9처럼 넣는데

init 방법 별로 step별로 각각 따로 만들어서 제시해야함. 전체를다.

A6 도 동일하게 B로 변경해서 모든경우를 다 넣어야함.

plot 도 본문과 같이 변경한다
