# Execution Scripts

Purpose: Guarded launcher scripts copied into the handoff bundle for reproducible post-checkpoint execution.

Status: `EXECUTION_DRAFT_NOT_FINAL`

Next gate: Use only after `READY_TO_LAUNCH=yes`; source of truth remains the repo-level `scripts/` directory.

Source of truth remains outside this bundle under `docs/exp/v5/`.
Rebuild this folder with `python3 scripts/build_v5_release_bundle.py`.
