# Tokenizer Training Logs

이 폴더는 v5 SentencePiece append tokenizer 학습 로그를 보관한다.

Use these logs only as execution provenance. Report/PPT 수치는
`../README.md`, `../../03_audit/`, and generated tokenizer audit tables에서
승격된 값만 사용한다.

Next step gate: tokenizer output and audit artifacts must exist before the
tokenizer result can support the main Glot500-style replay claim.
