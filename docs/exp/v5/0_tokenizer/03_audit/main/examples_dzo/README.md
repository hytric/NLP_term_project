# Main dzo_Tibt Examples

이 폴더는 main tokenizer에서 남은 `dzo_Tibt` fertility regression의 sentence-level
evidence를 보관한다.

Use this folder for limitation analysis, not for a positive tokenizer claim.
The key point is that the regression is not explained by `<unk>` or byte
fallback alone.
