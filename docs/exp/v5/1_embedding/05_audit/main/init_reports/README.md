# Main Init Reports

이 폴더는 main tokenizer 기반 initialized checkpoint의 raw `init_report.json`
files를 보관한다.

Report/PPT should cite summarized audit tables first, then use these JSON files
when exact row counts or remap evidence are needed.
