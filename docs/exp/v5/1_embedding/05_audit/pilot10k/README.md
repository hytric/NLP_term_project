# Pilot Initialization Audit

이 폴더는 pilot tokenizer initialized checkpoint audits를 보관한다.

Role: smoke-test evidence for the initialization pipeline. Final main-method
claims should use `../main/` instead.
