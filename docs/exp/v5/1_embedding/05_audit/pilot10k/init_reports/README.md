# Pilot Init Reports

이 폴더는 pilot tokenizer 기반 initialized checkpoint의 raw `init_report.json`
files를 보관한다.

Use these only for pilot history or debugging. Main novelty claims should use
the full-tokenizer reports under `../../main/init_reports/`.
