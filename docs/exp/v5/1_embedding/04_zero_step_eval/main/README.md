# Main Zero-Step Evaluation

이 폴더는 full v5 tokenizer 기반 `random`, `mean`, `fvt` initialized checkpoints의
pre-MLM MLM proxy 결과를 보관한다.

Main evidence:

- `results.md`
- `analysis.md`
- `summary.tsv`
- `scores.tsv`

Promotion rule: these rows support only the intrinsic zero-step initialization
claim. They do not prove after-MLM or downstream improvement.
