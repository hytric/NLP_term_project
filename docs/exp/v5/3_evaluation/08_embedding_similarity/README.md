# 08 Embedding Similarity

Additional novelty diagnostic.

Use this folder for sentence-vector similarity, same-language clustering, and
2D UMAP/t-SNE visualizations.

## Next Step Gate

This diagnostic can move to `../09_aggregation/` or `../../4_reporting/` only
after it is clearly labeled as additional analysis, not a required Glot500
metric.

Pass line:

- diagnostic question is written before plotting.
- sampled languages and sentences are recorded.
- model list matches the main comparison when possible.
- figure source data is saved.
- interpretation is tied to tokenizer or embedding initialization behavior.

Required artifacts:

- command log or notebook path
- source data table
- figure file
- short interpretation note

If this diagnostic conflicts with downstream metrics, present it as explanatory
evidence rather than replacing required metric results.
