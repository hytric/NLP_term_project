# POS glot500_base Output

이 폴더는 `cis-lmu/glot500-base` POS reference output을 보관한다.

Promotion rule: use aggregated F1 values and keep the local `TRAIN_LANGS=tur_Latn`
condition visible.
